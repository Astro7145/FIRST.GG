import { StyleSheet } from "react-native";

const recentStyle = StyleSheet.create({
    recent:
    {
        fontWeight: 'bold',
        color: 'black',
        fontSize: 30
    }
})

export default recentStyle